import { Routes } from '@angular/router';
import { GridDemoComponent } from './grid-demo/grid-demo.component';

export const routes: Routes = [
  { component: GridDemoComponent, path: 'grid-demo' },
];
